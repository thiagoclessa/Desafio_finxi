apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronrequest
  namespace: aplicacao
spec:
  schedule: "*/3 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: cronrequest
            image: busybox
            imagePullPolicy: IfNotPresent
            command:
            - /bin/sh
            - -c
            - wget -r http://ipaas:8080/actuator/health/
          restartPolicy: OnFailure
